# Fail fast if npm runs under a Node version outside `engines` (package.json).
# Prevents installing/building native modules (better-sqlite3, expo-sqlite) under
# the wrong Node ABI, which silently breaks tests/build with NODE_MODULE_VERSION
# mismatches. Pair with `.nvmrc` (run `nvm use` before installing).
engine-strict=true
